"use strict";Object.defineProperty(exports,"makeFilepath",{enumerable:true,get:function(){return makeFilepath}});const makeFilepath=(race_id,timestamp,place="")=>{const date=new Date(timestamp);const parent_dir=date.toLocaleDateString("ja-JP",{timeZone:"Asia/Tokyo",year:"numeric",month:"2-digit",day:"2-digit"});const target_dir=`${process.cwd()}/content/docs/${parent_dir}`;return place.length===0?`${target_dir}/${race_id}.mdx`:`${target_dir}/${place}/${race_id}.mdx`};